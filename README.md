# EduCraft Creators Site

Official static website for EduCraft Creators.

This repository is intended for Cloudflare Pages.

DB Bottleneck Analyzer product page: `db-bottleneck-analyzer/index.html` (`?lang=ja|en|es|id|ko`)

The legacy `pgscope/` route redirects to the new product URL. The product page is intentionally separate from the learning-app site and is excluded from search indexing while sales preparation is underway.
